(ns quickstart.helloworld
  (:require
   #_[sci.core :as sci]
   #_[sci.impl.vars]
   sci.impl.interpreter))

(defrecord Eita [a])

(defn main []
  (print (str "hello,ss world\n"
              (->Eita 10))))
